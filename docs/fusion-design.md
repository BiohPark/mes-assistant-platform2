# 융합 설계 — platform2 × Codex 비교와 보완

작성일: 2026-09-26 (KST) · 상태: **설계 문서 (구현 아님)**

이 문서는 `mes-assistant-platform2`(이 저장소, 기준)의 설계를, 형제 저장소 `mes-assistant-platform`(Codex 작업)의 **실제 코드**와 Codex 설계안(`docs/codex-demo-integration-design-comparison.md`, 구현되지 않은 아이디어 문서)에 비추어 보완한 결과다.

- 전제: 이 저장소는 **데모로 마무리**하고, 실제 구현은 **별도 저장소**에서 진행한다.
- 이 문서만으로 구현을 시작하지 않는다. 구현은 별도 요청과 실행 계획으로 진행한다.
- 검증 범위는 **모의 API 검증 + 실제 환경용 검증 절차 준비**까지다. 실제 OpenWebUI의 파일 이해·답변 품질을 검증했다고 해석하지 않는다. 절차서: [evaluation/real-env-verification.md](evaluation/real-env-verification.md)

## 0. 읽는 법

**근거 표기**

| 표기 | 의미 |
|---|---|
| **[기록]** | 과거 문서(HANDOFF 등)에 남은 검증 결과. 이번에 다시 실행하지 않았다 |
| **[확인]** | 이번 작업에서 소스 코드를 읽어 확인한 사실. **P2 `6389190`, CX `57207c0` 기준**. 테스트·빌드·브라우저는 실행하지 않았다 |
| **[추론]** | 코드에서 추론했으나 실행으로 확인하지 않은 것 |
| **[제안]** | 아직 구현하지 않은 설계 |

**경로 표기**: `P2 src/...` = 이 저장소, `CX src/...` = `mes-assistant-platform` 저장소 루트 앱(`legacy/`, `agent-hub-demo/` 제외). 줄 번호는 위 커밋 기준이다.

**최우선 원칙 — 사용자 정정** (CX 설계안 §2에서 사용자가 직접 선택한 내용)

- 단계·선후 관계·에이전트 순서를 참조 조건으로 쓰지 않는다. 순서 기반 인계, 고정 워크플로우, 자동 후속 실행을 만들지 않는다.
- **같은 태그를 직접 공유하는 대화를 파일처럼 통째로 체크**하고, 필요하면 메시지 선택·요약 같은 세부 조절을 한다.
- 대화를 체크하면 **전체 원문**이 기본이다. 한도를 넘으면 범위 선택이나 요약을 안내한다.
- **자동 절단·자동 요약·참조 대화의 재귀 수집을 하지 않는다.**

이전 platform2 계획의 "인계 메모"와 "다음 단계로 넘기기"는 이 원칙과 충돌하므로 폐기한다(§3).

---

## 1. 진행 방식 판단 — 데모 완성(경우 1) vs 바로 새 저장소(경우 2)

판단 기준은 데모에 들이는 작업이 새 저장소로 **이월되는가, 버려지는가**다.

| 이월됨 (새 저장소 프론트가 platform2 코드에서 출발할 경우) | 버려짐 (서버가 대체) |
|---|---|
| 화면·UX 흐름, React/shadcn 컴포넌트 | Dexie 스키마·마이그레이션·저장소 구현 |
| 도메인 순수 함수: `tags`·`sharedPool`·`kanban`·`titles`·`modelResolution`·`reporting` | 브라우저 lease/heartbeat/epoch, 탭 간 동시성 보호 |
| 프롬프트 조립(`llm/context.ts`), SSE 파서, 개발용 Mock provider | IndexedDB 백업 형식·복원 검증 |
| 평가 시나리오·테스트 케이스, 확정된 정책 | 브라우저의 API 키 보관, CORS에 의존하는 직접 호출 |
| 데이터 계약·DDL 초안(문서) | IndexedDB 파일 저장소 구현체 |

| | 경우 1: 데모 완성 → 테스트·보완 → 새 저장소 | 경우 2: 지금 바로 새 저장소 |
|---|---|---|
| 장점 | 새 UX(대화 선택·요청 트레이·크기 한도)를 현업과 적은 비용으로 검증. 데모가 살아 있는 명세가 됨 | 중복 투자 없음. 동시성·키·CORS·파일 저장이 서버에서 자연스럽게 해결됨 |
| 단점 | 저장 계층 정규화(약 75파일)와 요청 lease(약 24파일)가 대부분 버려짐 | 검증 안 된 UX를 전제로 백엔드 스키마를 설계할 위험 |
| 적합한 상황 | 새 저장소 착수가 수개월 뒤이고, 그동안 데모를 현업이 실제로 씀 | 같은 팀이 수주 안에 착수하고, UX를 새 저장소 첫 반복에서 검증해도 됨 |

**추천 — 경우 2를 기본으로, 짧은 UX 검증 스프린트를 거친다 (2-b)**

1. 데모에서는 **이월되는 것만** 한다(약 25–35파일).
   - 새 UX 프로토타입: 자료 패널의 "대화" 섹션, 컴포저 위 "이번 요청에 사용" 트레이와 크기 게이지, 답변별 "사용한 자료"와 앱 내 전송 기록 뷰어
   - 시연을 깨는 결함 4건 수정: 첨부가 1회만 전달됨, SR 첨부 누락, 산출물 버전 체인이 안 생김, 입력창 잠김
   - API 키가 백업에 포함되는 문제 차단
2. 저장 계층 전면 정규화, lease·fencing, 파일 저장소 구현체는 데모에서 하지 않는다. §8의 데이터 계약·DDL·API 문서로 옮기고 새 저장소에서 서버 기준으로 구현한다.
3. 현업 2–3명이 대표 과업(§6 E6)으로 시험해 미확정 정책(§5)을 확정한 뒤 데모를 동결한다(예: 태그 `demo-final`).
4. 새 저장소: 서버(예: Spring Boot) + DB + **파일은 디스크·NAS에 파일 그대로**. 프론트는 platform2의 `domain`·`llm`·`components`·`features`를 이식하고, `db/repositories`는 같은 시그니처의 API 클라이언트로 바꾼다.

경우 1을 그대로 하는 것은 "새 저장소 착수가 한참 뒤이고 데모를 실사용"할 때만 권한다.

---

## 2. 두 프로젝트 비교 — 코드 근거

### 2.1 파일 업로드와 AI 전달

| 항목 | platform2 | Codex | 판단 |
|---|---|---|---|
| 업로드와 선택의 분리 | 자료 패널 업로드는 보관만 한다. **컴포저 첨부는 그 메시지 1회만 참고 입력으로 전달**하고 `task.inputs`에 남기지 않아 다음 턴부터 빠진다. 안내도 없다(`P2 src/features/chat/useChat.ts:43-45`) [확인] | 업로드와 선택(TaskInput)이 분리되어 있고 자동 선택이 없다(`CX src/hub.ts:180-209`) [확인] | Codex 쪽이 일관적. platform2의 1회성은 결함 |
| OpenWebUI 처리 완료 | 업로드 직후 바로 첨부하고 처리 상태를 확인하지 않는다(`P2 src/llm/openwebuiFiles.ts:27-37`) [확인] | 2초 간격으로 처리 상태를 확인하고 파일당 최대 5분 기다린다(`CX src/app/openWebUI.ts:52-67`). 요청 전체 제한은 OpenWebUI 360초, 일반 API 60초(`CX src/app/requestService.ts:334,356`) [확인] | Codex 채택 |
| 복수 파일·취소·제한 시간 | 순차 업로드. 신호(signal)와 제한 시간이 없고, 업로드는 중지 컨트롤러가 만들어지기 전에 실행된다(`P2 useChat.ts:149-152`) [확인] | 순차 처리, 모든 요청에 신호 전달, 다른 탭의 취소를 heartbeat로 감지(`CX requestService.ts:229-251,295-305`) [확인] | Codex 채택 |
| 실패 처리 | 실패한 파일은 **자동으로 본문 인라인**(D10). 바이너리는 이름·형식·크기만 들어간다. 이 사실은 전송 기록 JSON에만 남는다(`P2 useChat.ts:78-87`, `src/llm/context.ts:26-29`) [확인] | 채팅을 호출하지 않고 요청을 `failed`로 남긴다. 복구는 사용자가 누른다: 현재 선택으로 재시도 / 파일 제외 / 텍스트로 재시도(`CX src/Workspace.tsx:636-653`). 단, 실패한 파일만이 아니라 요청의 모든 파일에 제외 버튼이 뜨고, 복구 버튼은 채팅 하단 한 곳에만 있다 [확인] | Codex 원칙을 **수정해서 채택** — 실패한 항목에만, 항목 옆에 |
| 텍스트 대체와 절단 | 파일 12,000자, SR 본문 4,000자를 **조용히 절단**(`P2 context.ts:6-7,27,93`) [확인] | 대화 요청에는 자동 절단이 없다. 직렬화한 요청 본문 바이트로 한도를 적용한다(기본 262144, `CX requestService.ts:95-101`) [확인] | Codex 원칙 채택, 절단 제거 |
| 한도 초과 UX | 한도 개념 없음 | 4.5초 뒤 사라지는 토스트만 뜨고, 요청 기록이 남지 않으며, 전송 전 크기 표시가 없다(`CX Workspace.tsx:214-215`, `src/store.tsx:41-45`) [확인] | 둘 다 부족 → 게이지 + 사라지지 않는 안내 [제안] |
| 주 입력(★) 전달 | OpenWebUI 모드에서도 system 메시지에 `[주 입력] 이름 vN — 출처 (첨부 파일로 전달)` 라벨을 보낸다(`P2 context.ts:44-53`). 다만 `files[]`는 선택 순서이고 업로드 이름에 버전이 없다(`P2 openwebuiFiles.ts:29`, `useChat.ts:45,85`) [확인] | OpenWebUI 모드에서는 참고 메시지를 생략하고 파일을 `type/id`로만 보내 ★가 전달되지 않는다(`CX src/api.ts:58-61`, `openWebUI.ts:75`, HANDOFF 9-B) [확인] | platform2 유지 + 순서·버전 이름 보완 |
| 전송 기록 | 요청 본문 JSON 문자열(OpenWebUI 파일 ID 포함)을 답변 메시지에 스트림이 끝난 뒤 저장한다(`P2 useChat.ts:156-181`). 탭이 죽으면 기록이 남지 않는다 [확인] | RequestRecord를 전송 **전**에 사용자 메시지와 함께 원자적으로 저장. 파일 버전·★·프로필·해시 기반 스냅샷을 남기지만 **원격 파일 ID와 `files` 배열은 기록하지 않는다**(`CX requestService.ts:126-183`, `openWebUI.ts:80`) [확인] | Codex의 구조 + platform2의 원격 ID 기록을 결합 |
| 화면에 첨부된 것 vs 실제로 받은 것 | 앱 안에서 볼 화면이 없고 JSON 다운로드만 있다(`P2 src/features/chat/MessageBubble.tsx:96-106`) [확인] | 입력창 위에 "다음 요청에 사용 · N개" 칩 줄이 있다(`CX src/MaterialLibrary.tsx:11-52`). 과거 전송 내용은 JSON 다운로드만 가능하다(`CX Workspace.tsx:552-584`) [확인] | 둘 다 부족 → 트레이 + 답변별 "사용한 자료" + 뷰어 [제안] |
| 재시도 | 없음 | 현재 선택으로 다시 구성한다(`CX requestService.ts:94,104-107`). 실패한 요청의 사용자 메시지가 이미 저장되어 이력으로 들어가고, 같은 프롬프트가 마지막 메시지로 다시 붙어 **AI에 두 번 전달될 수 있다**(`CX api.ts:42-63`) [추론] | 재시도는 채택, 중복 전송은 막는다 |

### 2.2 느슨하게 연결된 대화 컨텍스트

| 항목 | platform2 | Codex | 판단 |
|---|---|---|---|
| 후보 범위 | 태그를 직접 공유하는 대화만 "연결된 대화"로 보여 준다. 비교는 `toLowerCase`라 자료함의 `tagKey`와 기준이 다르고, **에이전트 순서로 정렬**한다(`P2 src/features/task/useTaskData.ts:45-50`) [확인] | 직접 태그 공유만 인정하고, A–B–C 간접 확장을 막는 테스트가 있다(`CX hub.ts:77-87`, `src/hub.test.ts:37`) [확인] | 규칙은 같다. `tagKey`로 통일하고, 정렬은 최근 활동순으로 바꿔 순서처럼 보이지 않게 한다 |
| 대화를 입력으로 선택 | 없다. 이동 링크(`P2 src/features/task/RelatedStrip.tsx`)만 있다 [확인] | **없다.** ContextPicker(896줄)는 켜는 호출이 없어 현재 UI에서 열 수 없다(`CX Workspace.tsx:47,953-960`). 다른 대화 내용을 쓰려면 "메시지를 자료로 저장 → 발견 → 체크"를 거쳐야 한다(`CX Workspace.tsx:525-538`) [확인] | 양쪽 모두 새로 설계 [제안] |
| 요약 | 없다 | "요약·전달 메모를 자료로 저장"은 사람이 직접 쓰는 것이다(`CX MaterialLibrary.tsx:279-315`) [확인] | 사용자가 요청할 때만 만드는 보조 요약 [제안] |
| 메시지 불변성 | 완료된 메시지는 수정하지 않는다. 스트리밍 중인 자리표시만 갱신되고, 대화·초안 SR 삭제 때만 지운다(`P2 src/db/repositories/chat.ts:63-71`, `tasks.ts:262`, `sr.ts:115`) [확인] | 추가만 하는 명령 구조 [확인] | 내용을 복사하지 않고 메시지 ID 경계로 고정할 수 있다 |

### 2.3 UI·UX

| 항목 | platform2 | Codex | 판단 |
|---|---|---|---|
| 대화 화면 | 채팅 + 오른쪽 탭 패널(자료·체크·노트·이력). 좁은 화면은 탭 전환. Markdown 렌더링과 SSE 스트리밍 [확인] | 3열, 탭 없음. 좁은 화면은 세로로 쌓인다. 평문 표시, `stream:false`. `Workspace.tsx` 1127줄 [확인] | platform2 유지 |
| 자료함 | 출처 에이전트별로 묶고 이전 버전은 접으며 "새 버전 있음 · 바꾸기" 표시. 검색·필터 없음(`P2 src/features/task/MaterialsPanel.tsx`) [확인] | 파일명 검색, 출처 에이전트·Input/Output·일치 태그 필터(`CX MaterialLibrary.tsx:166-273`) [확인] | Codex 필터 채택 |
| 칸반 | 열 미니맵 점프, 빈 열 전체 접기 [확인] | 열 개별 접기, 빈 열 숨기기, 사용자별 화면 설정(`CX src/HubHome.tsx:87-109,504-517`) [확인] | Codex 기능을 소규모로 채택 |
| 대화 시작 | 초안 → 첫 전송 때 생성 [확인] | 첫 저장을 한 트랜잭션에서 처리. 첨부는 1개, 첨부만 있으면 AI를 호출하지 않는다(`CX src/app/draftService.ts:24-114`) [확인] | platform2 유지. 원자성은 서버로 |
| SR 결과 공유 | SR 상세에서 공유하면 파일 후보가 없다(`P2 src/features/sr/SrDetailSheet.tsx:206` — task 없이 연다) [확인] | 이 업무의 자기 파일만 나열한다(`CX Workspace.tsx:1069-1124`) [확인] | 둘 다 보완: 후보 = 자기 파일 + 열람 가능한 선택 입력 |
| 오류 복구 위치 | 체크리스트 점검·완료·산출물 저장·가져오기·업로드에서 오류가 조용히 사라지는 곳이 있다 [확인] | 요청 오류는 채팅 하단 한 곳, 나머지는 토스트 [확인] | 항목 옆에 배치 [제안] |
| 입력 방식 | 공통 대화상자 컴포넌트 [확인] | `prompt` 5곳이 남아 있고, 모델 입력은 글자마다 저장·활동 기록(`CX Workspace.tsx:462-468`) [확인] | platform2 유지 |

### 2.4 저장 구조와 보호

| 항목 | platform2 | Codex | 판단 |
|---|---|---|---|
| 동시 변경 | 엔티티별 저장소 + liveQuery. 두 탭이 동시에 전송하는 것을 막지 못한다. 바쁨 판단은 화면에서 `streaming` 메시지가 있는지로만 한다(`P2 src/features/chat/ChatView.tsx:33`) [확인] | 모든 테이블 쓰기 트랜잭션에서 **전체 상태를 다시 읽고 reduce한 뒤 JSON 비교로 기록**한다(`CX src/db/commands.ts:33-91`, `src/db/schema.ts:55-123`). 손실은 없지만 매 동작 비용이 DB 전체에 비례하고 서버로 옮길 수 없다 [확인] | 원칙(요청 전 기록, 대화당 활성 1건)만 채택, 구조는 미채택 |
| 요청 스냅샷·lease | 없다. 화면을 벗어나면 요청이 취소된다(`P2 useChat.ts:109`). 프롬프트 생성이 try 밖이라 예외가 나면 자리표시가 영구 `streaming`으로 남아 입력창이 잠긴다(`P2 useChat.ts:146-158`) [확인] | 30초 lease + 10초 heartbeat, 만료 복구, 토큰 fencing(`CX requestService.ts:147,186-192,287-308`) [확인] | 새 저장소에서 서버 상태 기계로. 데모는 최소한만 |
| 백업·복원 | **API 키가 IndexedDB에 저장되고 백업 JSON에 그대로 포함된다**(`P2 src/db/repositories/settings.ts:16-19`, `src/db/schema.ts:53-65`, `src/db/exportImport.ts:34-48`). 복원 검사는 형식만 한다 [확인] | 금지 필드 재귀 검사, 참조·해시 검증, 100MB 상한, 진행 중 요청이 있으면 백업·복원 거부, 복원 epoch로 오래된 탭 차단(`CX src/db/backup.ts`, `src/db/validate.ts`, `src/store.tsx:77-82`) [확인] | 데모는 비밀값 제외만. 나머지는 서버 백업으로 대체 |
| 저장 위치 분리 | 파일 본문이 메타데이터 행 안의 blob이다. API 키는 DB, 화면 설정은 localStorage [확인] | 본문은 `blobs` 테이블(요청 스냅샷 조각과 같은 테이블), 텍스트는 `content`에도 이중 저장. 키·원격 파일 캐시는 메모리(`CX schema.ts:43`, `Workspace.tsx:141-142`, `openWebUI.ts:5`) [확인] | §8 분류표로 정리 |
| UI·저장 경계 | features에서 `db`를 직접 읽는 곳이 있다(`useChat`, `useTaskData`, `ReportsPage`) [확인] | 명령 경로와 서비스 함수의 직접 쓰기, 두 갈래(`CX requestService.ts:150-174`, `draftService.ts:66,108`) [확인] | 새 저장소에서 저장소 함수 = API 1:1 |

---

## 3. 채택 / 수정 채택 / 미채택

| 구분 | 항목 | 이유 |
|---|---|---|
| 채택 | OpenWebUI 처리 상태 확인, 요청 신호·제한 시간 | 처리가 끝나기 전에 첨부하는 불확실성 제거 |
| 채택 | 자동 절단 없음 + 요청 바이트 한도(설정 가능) | 사용자 원칙, 조용한 누락 방지 |
| 채택 | 요청 기록을 전송 전에 저장, 대화당 활성 요청 1건, 요청 중 완료 차단 | 입력창 잠김·두 탭 중복·기록 유실 방지 |
| 채택 | 자료함 검색·필터, 칸반 열 개별 접기, 공통 순서 편집의 revision 충돌 검사 | 적은 비용으로 다수 자료·다수 편집자 대응 |
| 채택 | 완료 잠금을 데이터 계층에서 강제 | platform2는 UI에서만 막는다 |
| 수정 채택 | 파일 전달 실패 → 요청 중단. **실패한 항목에만, 항목 옆에** 재시도·제외·(텍스트 파일만) 텍스트로 보내기 | 바이너리 자동 대체는 AI가 본문을 받지 못했는데 답하는 오해를 낳는다. Codex는 버튼 위치·대상이 불명확하다 |
| 수정 채택 | "다음 요청에 사용" 칩 → 파일과 대화를 함께 보여 주는 트레이 + 전달 방식·크기·게이지 | 전송 전 확인이 목적 |
| 수정 채택 | 재시도(현재 선택으로) → 트레이에서 확인 가능, 사용자 메시지는 한 번만 보냄 | Codex의 중복 전송 문제 회피 |
| 수정 채택 | 원격 파일 ID → **요청 기록에는 남기고**, 재사용 캐시는 따로 두어 백업에서 제외 | CX 설계안 §4.5, Codex 기록의 감사 공백 해소 |
| 수정 채택 | 권한 규칙 → 순수 함수 명세로 문서화하고 강제는 새 저장소 서버에서 | 데모에는 인증이 없다 |
| 미채택 | 전체 상태 reduce + JSON 비교 명령 파이프라인, 명령 영수증 | 비용이 DB 전체에 비례하고 서버로 옮길 수 없다 |
| 미채택 | ContextPicker·ContextBundle·Handoff 재활성화 | 도달할 수 없는 코드이고, D1(태그로 일원화)과 충돌한다 |
| 미채택 | 브라우저 복원 epoch·전체 교체 백업 검증(데모) | 서버 백업으로 대체된다. 데모에는 비밀값 제외만 |
| 미채택 | 텍스트 이중 저장, 해시 기반 스냅샷 조각 | 데모 규모에 과하다 |
| 미채택 | 순서 기반 인계, "인계 메모", "다음 단계로 넘기기", 연결 대화 목록 자동 포함 | 사용자 정정: 선택한 것만 전달하고 순서 개념을 두지 않는다 |

---

## 4. 이전 platform2 계획에서 달라지는 부분

| 구분 | 내용 |
|---|---|
| 폐기 | 인계 메모, 다음 단계로 넘기기, 플랫폼 컨텍스트에 연결된 대화 이름을 자동으로 넣는 것 |
| 대체 | **같은 태그의 대화를 입력으로 선택**(§5). 요약은 그 안의 선택 옵션이며 사용자가 요청할 때만 만든다 |
| 변경 | 12,000자 절단 → 요청 바이트 한도·게이지·전송 차단·안내 |
| 변경 | 파일 전달 실패: D10의 자동 인라인 대체 → 중단 + 항목별 선택. **D10 변경이라 사용자 확인 필요**(§6 정책 7) |
| 축소 | Dexie v4/v5 전면 정규화, 파일 저장소 구현, 백업 v3, lease·fencing 전체 → 경우 2에서는 문서(데이터 계약·DDL·API)로 이관. 경우 1에서도 대화 입력·요청 기록 두 테이블과 최소 복구만 |
| 유지 | SR 첨부 누락·산출물 버전 체인·입력창 잠김 수정, 컴포저 첨부 고정(§6 정책 8), Mock 응답의 "사용한 자료" 표시, 요청 상태줄, UI 다듬기(항상 보이는 액션, 로딩·오류 처리, 폰트, 다크 모드) |
| 추가 | 대화 후보와 선택 화면, 선택 시점 고정 + "새 메시지 N · 갱신", 크기 게이지, 앱 내 전송 기록 뷰어, 답변별 "사용한 자료", 실제 환경 검증 절차 문서 |

---

## 5. 대화 컨텍스트 설계 [제안]

### 5.1 후보

- 현재 대화와 `tagKey`를 하나 이상 **직접** 공유하는 다른 대화만 후보다. 자기 자신과 간접 연결(A–B, B–C만 있을 때 A에서 C)은 제외한다.
- 최근 활동순으로 보여 준다. 에이전트는 칩으로만 표시하고 "이전/다음 단계" 같은 표현은 쓰지 않는다.
- SR 접수 대화는 후보에서 제외한다. SR 본문과 첨부로 대신하고, 요청자와의 대화를 보호한다.
- 태그는 발견 조건이지 권한이 아니다. 새 저장소에서는 열람 권한을 서버에서 다시 검사한다.

### 5.2 선택 단위와 모드

파일과 같은 행 패턴(☑ 선택, ★ 주 입력, 출처, 크기, 미리보기)을 쓴다.

| 모드 | 내용 |
|---|---|
| `full` (기본) | 선택 시점까지의 적격 메시지 **전체 원문**. 적격 = 사용자·assistant 역할, 완료 상태, 팀 의견이 아닌 것. system·실패·미완성 응답은 제외 |
| `messages` | 선택 대화상자에서 개별 메시지 또는 "여기부터/여기까지" 범위를 고른다. 전달될 내용을 미리 보여 준다 |
| `summary` | 사용자가 누르면 보조 요청으로 만들고, 미리보기·편집을 거쳐 적용한다. 요약 요청 자체가 한도를 넘으면 메시지 범위를 줄이도록 안내한다. 자동 분할 요약은 하지 않는다. Mock에서는 "대역"으로 표시한 규칙 추출 |

### 5.3 고정과 갱신

- 선택하면 `ContextSnapshot`을 만든다.
  - `full`·`messages` 모드는 메시지 ID 목록과 경계(`upToMessageId`)만 저장하고 **내용은 복사하지 않는다**. 완료된 메시지는 바뀌지 않기 때문이다(§2.2).
  - `summary` 모드는 요약 본문과 출처(원본 메시지 ID·모델·작성자·수정자)를 저장한다.
- 원본 대화에 새 메시지가 생기면 "새 메시지 N · 갱신"을 표시한다. 갱신하면 새 스냅샷을 만든다.
- 요청을 시작할 때 사용한 스냅샷 ID를 요청 기록에 고정한다. 선택 해제나 갱신은 다음 요청부터 반영된다.

### 5.4 재귀 금지와 한계

- 참조 대화 **자신의 메시지만** 전달한다. 그 대화가 입력으로 쓴 파일, 그 대화가 참조한 다른 대화, 요청 스냅샷, system 프롬프트는 모으지 않는다. 필요한 파일은 사용자가 따로 선택한다.
- 한계: 원본 답변에 이미 다른 대화의 내용이 섞여 있을 수 있다. 이 설계는 그런 간접 전파까지 막거나 정보 출처를 완전히 증명하지 않는다.

### 5.5 프롬프트 배치

- 현재 대화의 발화로 합치지 않는다. 주입 가드(`INJECTION_GUARD`) 뒤의 `## 참조 대화 (N)` 절에 참고 데이터로 넣는다.
- 머리글: `### [주 입력|참고] WK-… · 에이전트 · "제목" — 메시지 12개 · 선택 시점 …`
- 본문: `[사용자 · 이름] …` / `[assistant] …`. 항상 인라인이다. 원문 전체를 전달하는 것이 목적이므로, 검색 기반 부분 전달이 될 수 있는 파일 첨부 방식은 쓰지 않는다.
- 업무 역할 지침은 넣지 않는다(D9).

### 5.6 삭제 보호

다른 대화가 입력으로 참조하는 대화는 삭제를 막는다. 입력으로 쓰이는 파일에 적용하는 규칙과 같다.

### 5.7 데이터 모델 초안

| 모델 | 필드 | 비고 |
|---|---|---|
| `ConversationInput` | `id, taskId, sourceTaskId, weight('main'\|'reference'), mode, snapshotId, sortOrder, selectedBy, selectedAt` | unique(`taskId`, `sourceTaskId`) |
| `ContextSnapshot` | `id, sourceTaskId, mode, messageIds[], upToMessageId, bytes, summaryText?, summaryModel?, createdBy, createdAt, editedBy?, editedAt?` | 불변. 갱신 = 새 행. DB에서는 `messageIds[]` → 자식 테이블 `context_snapshot_message(snapshot_id, message_id, seq)` |
| `RequestRecord.inputs[]` | 공통 `kind('file'\|'conversation')` / 파일: `fileId, version, weight, source, oneShot, delivery('attached'\|'inline'\|'metadata_only'\|'failed'), remoteId?, error?` / 대화: `sourceTaskId, snapshotId, mode, messageCount, bytes` | 요청 시작 시 고정. API 키 없음 |

### 5.8 크기 한도

- 직렬화한 요청 본문 전체의 바이트로 잰다. 기본 256 KiB이고 설정에서 조정할 수 있다. OpenWebUI에 첨부로 올리는 파일 바이트는 여기에 포함되지 않는다고 표시한다.
- 컴포저 위 트레이의 게이지는 선택 항목·작성 중인 글·대화 이력으로 실시간 계산한다(예: `예상 180 KB / 256 KB`).
- 한도를 넘으면 전송 버튼을 막고 사라지지 않는 안내를 보여 준다. 동작: [입력 해제] [메시지 범위 선택] [요약 만들기] [새 대화로 이어가기(이 대화를 참조로 선택)]
- 바이트 한도는 모델의 토큰 한도와 다르다. 한도를 통과했다고 모델 컨텍스트에 들어간다고 보장하지 않는다.

### 5.9 화면

| 위치 | 내용 |
|---|---|
| 자료 탭 | "파일 \| 대화" 두 섹션. 같은 행 패턴. 대화 행: 에이전트 칩·코드·제목·상태·공유 태그·메시지 수·크기·"새 메시지 N"·"태그 해제됨" |
| 대화 선택 대화상자 | 모드(전체 / 메시지 선택 / 요약), 메시지 목록과 범위 지정, 전달 내용 미리보기, 크기. 좁은 화면에서는 전체 화면 시트 |
| 컴포저 위 트레이 | "이번 요청에 사용 · N" — 파일과 대화 칩(★/☑ 전환, ×), 전달 방식 배지(첨부/본문/이름만), 크기 게이지, 오류 항목 옆 복구 버튼. 좁은 화면에서는 "자료 N" 칩으로 접힘 |
| 답변 아래 | "사용한 자료" — 요청 기록에서 실제로 전달된 항목과 방식·실패를 보여 준다. 누르면 앱 안에서 전송 기록 뷰어가 열린다(원본 JSON 다운로드는 보조 기능) |

---

## 6. 미확정 정책과 추천 기본값

| # | 정책 | 선택지와 장단점 | 추천 기본값 |
|---|---|---|---|
| 1 | 대화 선택 시점 | **고정**: 미리 본 것이 곧 전송되고 재현할 수 있다. 파일 버전 고정(D6)과 일관된다. 갱신을 잊을 수 있다 / **전송 시 최신**: 단순하고 항상 최신이지만 미리보기와 실제 전송이 달라지고 진행 중인 원본이 섞인다 | 고정 + "새 메시지 N · 갱신" |
| 2 | 태그 해제 후 선택 | **유지**: 예기치 않은 유실이 없지만 관계없어진 자료가 계속 갈 수 있다 / **자동 해제**: 깔끔하지만 사용자가 모르게 사라진다 | 유지 + "태그 해제됨" 배지. 전송 시 존재·열람 가능 여부를 다시 검사 |
| 3 | 적격 메시지 | 팀 의견 포함 여부 | 제외(현재 대화의 이력 규칙과 같게) |
| 4 | 대화에 ★ 허용 | 파일과 같은 패턴 | 허용, 여러 개 가능(두 프로젝트 모두 복수 ★) |
| 5 | 한도 단위 | 바이트(단순) / 토큰(모델별 토크나이저 필요) | 바이트 256 KiB. 토큰 예산은 미확정으로 남긴다 |
| 6 | 요약 보관 위치 | 스냅샷 / 파일(산출물) | 스냅샷. 파일로 공유하려면 기존 "산출물로 저장"을 쓴다 |
| 7 | 파일 전달 실패 | **자동 인라인(D10)**: 편하지만 AI가 받은 것이 달라진다 / **중단 + 선택**: 정직하지만 한 번 더 눌러야 한다 | 중단 + 실패 항목 옆에서 선택. **D10 변경 — 사용자 확인 필요** |
| 8 | 컴포저 첨부 | 이번 메시지만 / 대화 입력으로 고정 | 고정(☑) + 칩에서 "이번 메시지만"으로 전환. OpenWebUI 채팅에서도 첨부가 대화 동안 유지되는 것과 같다 |
| 9 | 재시도 입력 | 실패 당시 스냅샷 / 현재 선택 | 현재 선택(트레이에서 보인다). 사용자 메시지는 재사용해 한 번만 보낸다 |
| 10 | 원격 파일 ID | 기록하지 않음 / 감사 기록 | 요청 기록에는 보존. 재사용 캐시는 따로 두고 백업에서 제외 |
| 11 | OpenWebUI 업로드 시점 | 전송할 때 / 선택할 때 미리 | 전송할 때. 미리 처리 여부는 실제 환경에서 처리 시간을 잰 뒤 판단 |
| 12 | 후보 정렬 | 에이전트 순서 / 최근 활동 | 최근 활동(순서처럼 보이지 않게) |

---

## 7. 기능 평가 순서와 완료 기준

모의 평가는 가상 자료, Mock provider, 가짜 OpenWebUI 경로(Playwright `route`)로 하고, 가로챈 요청 본문으로 판정한다. 스크린샷은 저장소 밖에 둔다.

| 순서 | 평가 | 완료 기준 |
|---|---|---|
| E1 | 현행 기준선 — 수정 전 데모로 시나리오 실행: S1 첨부 후 두 번째 턴 / S2 SR 첨부 / S3 기본 이름 산출물 2회 저장 / S4 태그 공유 → ★·☑ → 전송 / S5 OpenWebUI 모드 파일 순서·이름·처리 대기 / S6 프롬프트 생성 실패 / S7 두 탭 동시 전송 / S8 Mock 응답의 자료 인지 | 시나리오마다 결과와 요청 본문 증거를 남긴다. 코드로 확인한 결함(S1·S2·S3·S6)이 재현되는지 기록한다 |
| E2 | 파일 전달 — 업로드 → 선택 → 전송 → 기록. 복수 파일, 처리 대기, 실패·취소·재시도·제외·텍스트로 보내기, ★ 순서, 버전 이름 | 요청 본문의 파일 목록·순서·라벨이 트레이 표시와 같다. 전송 기록이 요청 본문과 일치한다. 실패해도 작성 중인 글과 선택이 보존된다 |
| E3 | 대화 컨텍스트 — 후보(직접 공유만), 통째 선택, 메시지 범위, 요약 적용, 갱신, 태그 해제, 상호 참조, 재귀 없음 | 선택한 스냅샷의 메시지만 요청에 들어간다. 팀 의견·실패 응답이 빠진다. 간접 후보가 없다. 참조 대화의 파일·참조가 따라오지 않는다. 상호 참조가 중복·무한 확장을 일으키지 않는다 |
| E4 | 한도와 요약 — 한도 초과, 요약 요청의 한도 초과, 요약 실패·취소 | 초과하면 전송이 막히고 입력·선택이 보존된다. 자동으로 잘리거나 요약되지 않는다 |
| E5 | 보호 — 프롬프트 생성 실패, 두 탭 동시 전송, 요청 중 선택 변경·화면 이동, 백업에 비밀값 없음 | 입력창이 잠기지 않는다. 활성 요청은 1건이다. 이미 보낸 요청의 기록은 바뀌지 않는다. 내보낸 JSON에 키가 없다 |
| E6 | UX 과업 — 1280/375px에서 대표 과업 5개: 자료 찾기·선택, 대화 선택, 전송 전 확인, 실패 복구, SR 결과 공유 | JSON을 열지 않고도 "무엇이 가는지"(전송 전)와 "무엇이 갔는지"(전송 후)를 답할 수 있다. 과업별 화면 이동 수·혼동 지점을 기록한다 |
| E7 | 실제 환경 검증 **절차 준비** (실행 안 함) | [evaluation/real-env-verification.md](evaluation/real-env-verification.md) 완성: 전제조건, Files API·처리 상태·`files` 파라미터 확인, 전체 전달과 검색 기반 전달의 차이, ★ 의미, 복수 파일 처리 시간, 가상 자료와 정답 근거가 있는 질문, 판정 기준, 기록 양식 |

실제 환경은 "절차 준비"까지만 완료로 본다. 모의 API 통과를 실제 연결 성공이나 파일 이해 품질 검증으로 쓰지 않는다.

---

## 8. 저장 구조 — 지금 정리할 가치가 있는 범위

| 데이터 분류 | platform2 현재 [확인] | Codex 현재 [확인] | 데모 권장 [제안] | 새 저장소 [제안] |
|---|---|---|---|---|
| 영속 업무 데이터 | Dexie 엔티티 테이블 | Dexie 엔티티 19개 테이블 | 유지 | DB 테이블(DDL 초안) |
| 파일 본문 | `files` 행 안의 blob | `blobs` 테이블 + 텍스트 이중 저장 | 현행 유지(경우 2) | 디스크·NAS에 **파일 그대로**, DB에는 `storage_key`·크기·해시만 |
| 요청 기록·스냅샷 | 답변 메시지의 JSON 문자열 | RequestRecord + 해시 조각 | RequestRecord 1개 테이블(경우 1에 한함) | 서버 테이블 |
| 화면 설정 | localStorage(zustand) | localStorage 사용자별 | 유지 | 클라이언트 유지 |
| 탭 세션(현재 사용자) | sessionStorage | sessionStorage(역할) | 유지 | 인증 세션 |
| 비밀값(API 키) | **DB + 백업** | 메모리(새로고침 시 재입력) | sessionStorage, 백업 제외 | 서버 비밀 저장소 |
| 원격 파일 재사용 캐시 | `files.remoteIds`(서버 주소 키만) | 메모리(서버·키·사용자 해시 범위) | 백업 제외 | 서버 테이블 |
| 실행 상태(lease·단계) | 메시지 `status` | 업무 레코드에 섞임 | 최소화 | 서버 작업 상태 |

**지금 문서로 확정할 가치가 있는 것**

- 데이터 계약: 엔티티·관계·불변 조건·위 영속 분류
- 대화 입력·스냅샷 모델, 요청 기록 명세(§5.7)
- 파일 저장 경계: 파일은 파일 그대로 보관하고, 업무 코드는 `fileId`로만 다루며, 바이트 접근은 한 곳에 모은다
- 저장소 함수 ↔ REST 매핑표, DDL 초안

**새 저장소로 미루는 것**: 코드 수준 정규화, lease·fencing, 복원 epoch, 백업 검증 — 서버 트랜잭션·고유 제약·DB 백업이 대신한다.

---

## 9. 과거 비교 문서와 현재 코드 대조

`mes-assistant-platform/docs/comparison-platform2.md`(2026-09-22)는 당시 `agent-hub-demo/`와 패키지 모델 시절의 platform2를 다룬다. 현재 코드와 대조한 결과다 [확인].

**Codex(A) 항목**

| 항목 | 현재 상태 | 근거 |
|---|---|---|
| P1-1 탭 간 덮어쓰기 | 해결 | `CX commands.ts:33-91`, `src/app/session.ts` |
| P1-2 응답 대기 중 컨텍스트 제거 시 기록 거절 | 해결 | `CX requestService.ts:150-165` |
| P1-3 중복 저장·무제한 요청 크기 | 부분 해결 — 텍스트 이중 저장, 전송 전 크기 안내 없음 | `CX Workspace.tsx:141-142`, `requestService.ts:95-101` |
| P2-1 완료 후 체크리스트 수정 | 해결 | `CX src/domain.ts:193-226` |
| P2-2 기간 리포트 부정확 | 여전히 존재 | `CX src/App.tsx:283-290` |
| P2-3 업로드와 AI 입력 선택 분리 | 해결 | `CX hub.ts:180-209` |
| P2-4 백업 복원·오류 복구 | 대부분 해결 — 오류 화면에서 원본 다운로드 없음 | `CX backup.ts`, `store.tsx:169-207` |
| P2-5 연결 진단·실패 UX | 부분 해결 — 스트리밍·연결 테스트 없음, 한도 초과는 토스트 | `CX Workspace.tsx:602-682` |
| P3 화면·코드 구조 | 대부분 미해결 — `Workspace.tsx` 1127줄, `prompt` 5곳, Markdown 없음 | — |

**platform2(B) 항목**

| 항목 | 현재 상태 | 근거 |
|---|---|---|
| API 키 영구 저장·백업 포함 | **여전함** | `P2 settings.ts:16-19`, `schema.ts:53-65`, `exportImport.ts:34-48` |
| 요청자 공개 경계 부족 | 부분 변화 — SharedResult 모델이 생겼으나 대화 화면에 열람 검사가 없다 | `P2 src/domain/types.ts:211-218`, `useTaskData.ts:25-62` |
| SR 프롬프트가 질문 방식을 지정 | 제거됨(D9). 첨부는 여전히 system 메시지에 인라인 | `P2 context.ts:108-116` |
| 패키지 수신 기록 삭제·스레드 물리 삭제 | 패키지 모델 제거(D1)로 해당 없음. `deleteThread`는 남아 있으나 호출되지 않음 | `P2 chat.ts:67` |
| 원자적 전송 잠금 없음 | 여전함 | `P2 ChatView.tsx:33` |
| 12,000자 단순 절단 | 여전함 | `P2 context.ts:6,27` |
| 주 JS 1MB 이상 | [기록] HANDOFF 기준 500KB 경고. 이번에 측정하지 않음 | — |
| 테스트 45개, `test` 스크립트 없음 | [기록] HANDOFF 기준 110개, 이번에 실행하지 않음. `test` 스크립트는 여전히 없음 | `P2 package.json` |

---

## 10. 데모 반영 현황 (2-b UX 검증 스프린트, 브랜치 `feat/ux-sprint`)

사용자 승인(추천대로 진행)으로 §1의 2-b를 데모에 반영했다. 아래는 **구현·검증된 것**이며, 검증은 모의 API·가상 자료 기준이다([evaluation/context-flow.md](evaluation/context-flow.md)).

| 설계 | 반영 | 코드 |
|---|---|---|
| §5 같은 태그 대화를 입력으로 선택 (전체·메시지 선택·요약, 스냅샷 고정, 갱신 안내, 재귀 없음, 삭제 보호) | 완료 | `src/domain/conversationContext.ts`, `src/db/repositories/conversationInputs.ts`, `src/features/task/ConversationInputs.tsx`, `ConversationPickerDialog.tsx` |
| 조용한 절단 제거 + 요청 바이트 한도(기본 256 KiB, 설정) + 전송 차단·안내 | 완료 | `src/domain/requestBudget.ts`, `src/llm/context.ts`, `src/features/chat/ContextTray.tsx` |
| 트레이("이번 요청에 사용")·답변별 "사용한 자료"·앱 내 전송 기록 | 완료 | `src/llm/promptBuilder.ts`(실제 전송과 같은 조립), `useRequestEstimate.ts`, `MessageBubble.tsx`, `RequestInfoDialog.tsx` |
| OpenWebUI 처리 완료 확인·버전 이름·주 입력 먼저·중지 신호, 실패 시 중단 + 항목별 복구 (정책 7) | 완료 | `src/llm/openwebuiFiles.ts`, `src/app/chatRunner.ts` |
| 대화당 진행 중 요청 1건·생존 신호·끊긴 응답 정리·시간 제한·재시도(사용자 메시지 재사용)·응답 중 완료 차단·화면 이동해도 요청 유지 | 완료 (데모 최소판 — 서버판은 새 저장소) | `src/app/chatRunner.ts`, `src/db/repositories/chat.ts` |
| 컴포저 첨부 입력 고정(정책 8), SR 첨부 전송 시점 재조회, 산출물 버전 체인 | 완료 | `useChat.ts`, `Composer.tsx`, `promptBuilder.ts`, `SaveAsOutputDialog.tsx` |
| 백업에 API 키·원격 파일 ID 없음, 가져오기 시 로컬 키·사용자 유지 | 완료 | `src/db/exportImport.ts` |
| Mock "사용한 자료" 블록, 항상 보이는 액션(포커스·터치), 조용한 실패 제거, SR 결과 공유 파일 후보 | 완료 | 각 화면 |
| 데이터 계약·DDL·API 대응표 (§8) | 문서 완료 | [architecture/data-contract.md](architecture/data-contract.md), [architecture/postgres-draft.sql](architecture/postgres-draft.sql) |
| 폰트(Pretendard)·다크 모드·자료함 외 칸반 열 개별 접기 | **하지 않음** | 새 저장소로 이월되는 시각 작업이지만 검증 목적과 무관해 보류 |
| 현업 UX 시험 → 정책 확정 → 데모 동결 | **남음** | [evaluation/ux-test-script.md](evaluation/ux-test-script.md) |

## 11. 관련 문서

- [HANDOFF.md](HANDOFF.md) — 현재 상태·결정 로그·미결정 항목
- [conversation-hub.md](conversation-hub.md) — 현행 대화·태그 모델 설계
- [evaluation/real-env-verification.md](evaluation/real-env-verification.md) — 실제 환경 검증 절차서(양식)
- 형제 저장소 `mes-assistant-platform`: `HANDOFF.md`, `docs/codex-demo-integration-design-comparison.md`, `docs/comparison-platform2.md` (읽기 전용 참고)
